import './app.css';

import { Route, Link } from 'react-router-dom';
import { useSelector } from 'react-redux';
// import { getAuthState } from './auth/auth.slice';

export function App() {
  // const auth = useSelector(getAuthState)
  // console.log(auth);
  
  return (
    <>
     admin dashboard
    </>
  );
}

export default App;
